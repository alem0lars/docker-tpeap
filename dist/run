#!/bin/bash

function stopTpeap {
  /opt/tplink/EAPController/bin/control.sh stop
  exit
}

trap stopTpeap EXIT

echo "Starting tp-link EAP controller (ignore fake msg 'Start failed.').." 1>&2
/opt/tplink/EAPController/bin/control.sh start

while true; do
  sleep 8
done
